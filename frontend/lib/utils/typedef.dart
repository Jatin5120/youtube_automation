import 'package:flutter/widgets.dart';
import 'package:frontend/app.dart';

typedef NavButtonWrapper = ({String label, VoidCallback onTap});

typedef ChannelSearchResult = ({List<ChannelModel> channels, String pageToken});
